<UserControl
    x:Class="AlarmServer.ConsoleControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlarmServer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Margin="10,0,10,0" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="80" />
        </Grid.RowDefinitions>

        <Grid Margin="0,30,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button Content="query" Command="{Binding StatusQueryCommand}" />
            <Button Content="close" Command="{Binding CloseAllConnectionsCommand}" Grid.Column="1" HorizontalAlignment="Right" />
        </Grid>

        <ListView ItemsSource="{Binding Events}" Grid.Row="1" Margin="0,20,0,0" Style="{StaticResource ConsoleListViewStyle}" />
    </Grid>
</UserControl>
